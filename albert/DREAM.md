# DREAM.md — compatibility shim

The old “dream cycle” prompt has moved to `jobs/NIGHTLY_REVIEW.md`.

Use that file for all nightly local review instructions. This file stays only so
old commands or references fail soft instead of recreating the old prompt
mythology.
