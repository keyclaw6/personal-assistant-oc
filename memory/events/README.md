# Event Logs

Append one JSON object per line:

```json
{"time":"2026-04-25T10:00:00+02:00","type":"preference","summary":"Kristian prefers file-based memory.","source":"conversation","confidence":0.8}
```

These logs are raw memory. Promote only durable items into `memory-wiki/`.
