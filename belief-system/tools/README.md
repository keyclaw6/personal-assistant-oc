# Tools

These helper scripts scaffold files. They do not replace the agent protocol.

- `new-session.ps1`: creates a session folder from templates.
- `new-belief.ps1`: creates a belief file from the belief template.

